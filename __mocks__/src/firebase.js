// __mocks__/src/firebase.js

// Mock the value of isFirebaseEnabled
export const isFirebaseEnabled = false; 

// Mock other exports from src/firebase.js if they are used elsewhere
// For example:
export const auth = {};
export const db = {};
